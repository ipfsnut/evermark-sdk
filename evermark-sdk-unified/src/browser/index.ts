/**
 * Browser module exports
 */

export { ImageLoader } from './image-loader';

// Re-export types from core for convenience
export type {
  ImageLoaderOptions,
  LoadImageResult,
  LoadAttempt,
  ImageSource
} from '../core/types';